# SPDX-License-Identifier: CC0-1.0
---
mode: required
method: legacy

labels:
  step: "Bootloader"

default: systemd-boot

items:
  - id: systemd-boot
    name: "systemd-boot"
    efiOnly: true
    description: "AgentOS uses systemd-boot exclusively."
